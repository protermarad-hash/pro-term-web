{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "PRO TERM: Deschide site-ul local",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --hostname 127.0.0.1",
      "cwd": "${workspaceFolder}",
      "serverReadyAction": {
        "pattern": "Local:\\s+(https?://\\S+)",
        "uriFormat": "%s",
        "action": "openExternally"
      }
    }
  ]
}
